# 📋 گزارش جامع پروژه Caremon (کا‌رِمون)

> **تاریخ گزارش:** ۱۲ فوریه ۲۰۲۵  
> **پلتفرم:** اجاره خودرو و رزرو آنلاین ماشین در ایران

---

## ۱. خلاصه پروژه

| مورد | وضعیت |
|------|--------|
| **تکنولوژی** | Next.js 14, React 18, TypeScript, Prisma, PostgreSQL |
| **استایل** | CSS Modules, CSS Variables (تم روشن/تاریک) |
| **منطق جستجو** | فیلتر و مرتب‌سازی سمت کلاینت (Client-side) |
| **دیتای خودروها** | Hardcoded در `src/db/cars.ts` (بدون دیتابیس) |
| **احراز هویت** | UI آماده، بدون منطق و API |

---

## ۲. 🔴 مشکلات بحرانی (Critical Issues)

### ۲.۱ امنیت

| مشکل | توضیحات | راه‌حل پیشنهادی |
|------|---------|-----------------|
| **🔴 فایل `.env` و رمز دیتابیس** | `DATABASE_URL` با اطلاعات واقعی در پروژه دیده می‌شود. `.env` در `.gitignore` نیست و احتمالاً commit شده. | ۱. اضافه کردن `.env` به `.gitignore`<br>۲. تعویض رمز دیتابیس Neon<br>۳. استفاده از `DATABASE_URL` فقط در envهای سرور (مثل Vercel) |
| **رمز عبور plain text در seed** | در `prisma/seed.ts` رمز عبور `"123456"` بدون hash ذخیره می‌شود. | استفاده از bcrypt/argon2 برای hash قبل از ذخیره |

### ۲.۲ باگ‌های کد

| مشکل | فایل | توضیحات |
|------|------|---------|
| **Typo در directive** | `src/app/dashboard/page.tsx` | `"use cleint"` باید `"use client"` باشد |
| **نمایش اشتباه تعداد نظرات** | `src/app/cars/[id]/components/car-info/car-info.component.tsx` | برای تعداد نظرات از `car.ratingNumber` استفاده شده؛ باید `car.reviewCount` باشد |
| **متغیر تعریف نشده در CSS** | `src/app/globals.css` | `--color-gray-50` استفاده شده اما تعریف نشده |

### ۲.۳ صفحات و لینک‌های گم‌شده

| مورد | توضیحات |
|------|---------|
| **صفحه فراموشی رمز عبور** | لینک `/auth/forgot-password` در Sign-in وجود دارد اما صفحه‌ای برای آن تعریف نشده |
| **صفحه رزرو** | لینک `/reserve/[carId]` در نتایج جستجو وجود دارد اما صفحه رزرو وجود ندارد |

---

## ۳. ⚠️ مشکلات متوسط (Medium Issues)

### ۳.۱ عملکرد و تجربه کاربری

| مشکل | توضیحات |
|------|---------|
| **Global Search Box بدون عملکرد** | input جستجو هیچ `onChange`/`onSubmit` ندارد و جستجو انجام نمی‌شود |
| **عدم همگام‌سازی فیلترها با URL** | فیلترها در state هستند؛ با refresh از بین می‌روند و share کردن لینک با فیلتر ممکن نیست |
| **Hydration در Dark Mode** | dark mode از `localStorage` خوانده می‌شود و باعث اختلاف بین server و client و احتمالاً flash می‌شود |

### ۳.۲ معماری و طراحی

| مشکل | توضیحات |
|------|---------|
| **عدم استفاده از دیتابیس برای خودروها** | خودروها در `src/db/cars.ts` هستند؛ Prisma فقط برای User استفاده شده |
| **عدم استفاده از Prisma در سرویس خودروها** | مدل Car در schema نیست و از Prisma استفاده نمی‌شود |
| **عدم وجود API Route** | هیچ `app/api/` برای auth، cars، reserve و غیره تعریف نشده |

### ۳.۳ محتوا و نگارش

| مشکل | فایل | نمونه |
|------|------|--------|
| **اشتباهات املایی/تایپی در Rules** | `src/app/Rules/page.tsx` | «فاضل» به‌جای «علاوه»، «مصرف نامحجم» به‌جای «استفاده نامتعارف»، «بلکه» به‌جای «بنزین» و... |

---

## ۴. 📦 ساختار فعلی پروژه

```
maincaremon/
├── src/
│   ├── app/
│   │   ├── auth/signin, signup     # UI بدون منطق
│   │   ├── cars/[id]               # جزئیات خودرو
│   │   ├── dashboard               # صفحه خالی
│   │   ├── questions               # FAQ
│   │   ├── Rules                   # قوانین
│   │   └── search                  # جستجو و فیلتر
│   ├── components/
│   ├── db/cars.ts                  # دیتای ثابت خودروها
│   ├── icons/
│   ├── lib/prisma.ts
│   ├── models/
│   └── types/
├── prisma/
│   ├── schema.prisma               # فقط مدل User
│   └── seed.ts
└── ...
```

---

## ۵. 🚀 فیچرهای پیشنهادی و برنامه آینده

### فاز ۱ – رفع بحران (۱–۲ هفته)

- [ ] اصلاح `.gitignore` و تعویض رمز دیتابیس
- [ ] اصلاح باگ‌های Typo، reviewCount و `--color-gray-50`
- [ ] Hash کردن رمز در seed
- [ ] اضافه کردن صفحات `forgot-password` و `reserve/[id]` (حداقل placeholder)

### فاز ۲ – احراز هویت (۲–۳ هفته)

- [ ] API routes برای signin/signup (NextAuth.js یا custom)
- [ ] JWT یا session management
- [ ] OAuth (مثل Google)
- [ ] صفحه forgot-password با ارسال ایمیل
- [ ] محافظت از مسیرهایی مثل dashboard

### فاز ۳ – رزرو و پرداخت (۳–۴ هفته)

- [ ] صفحه رزرو با انتخاب تاریخ و مدت اجاره
- [ ] درگاه پرداخت (زرین‌پال / سامان / ...)
- [ ] ذخیره رزرو در دیتابیس
- [ ] مدیریت وضعیت رزرو (تأیید، لغو، تکمیل)

### فاز ۴ – مدیریت خودروها (۲–۳ هفته)

- [ ] مدل Car در Prisma
- [ ] Migration و seed برای خودروها
- [ ] API برای CRUD خودروها
- [ ] پنل ادمین برای افزودن/ویرایش خودرو

### فاز ۵ – تجربه کاربری (۱–۲ هفته)

- [ ] اتصال Global Search به URL و فیلترها
- [ ] همگام‌سازی فیلترها با query string
- [ ] بهبود Dark Mode برای جلوگیری از hydration issue
- [ ] صفحه نتایج خالی و حالت‌های loading بهتر

### فاز ۶ – نظرات و امتیاز (۱–۲ هفته)

- [ ] مدل Comment در دیتابیس
- [ ] API ثبت/ویرایش/حذف نظر
- [ ] محاسبه میانگین امتیاز از روی نظرات واقعی

### فاز ۷ – مقیاس‌پذیری و کیفیت (مستمر)

- [ ] تست‌های واحد و E2E (Jest, Playwright)
- [ ] بهبود SEO (metadata, sitemap, schema.org)
- [ ] پوشش خطا و retry برای APIها
- [ ] مانیتورینگ و لاگ‌گیری

---

## ۶. فهرست صفحات و وضعیت آن‌ها

| مسیر | وضعیت | توضیح |
|------|--------|-------|
| `/` | ✅ | صفحه اصلی با جستجو |
| `/search` | ✅ | جستجو و فیلتر |
| `/cars/[id]` | ✅ | جزئیات خودرو |
| `/auth/signin` | ⚠️ | فقط UI، بدون منطق |
| `/auth/signup` | ⚠️ | فقط UI، بدون منطق |
| `/auth/forgot-password` | ❌ | وجود ندارد |
| `/reserve/[id]` | ❌ | وجود ندارد |
| `/dashboard` | ⚠️ | صفحه خالی |
| `/questions` | ✅ | FAQ |
| `/Rules` | ✅ | قوانین (با اشتباهات املایی) |

---

## ۷. توصیه‌های فنی

1. **Next.js Images**: فعلاً فقط `cafeerent.com` در config است؛ در صورت نیاز به دامنه‌های دیگر، `next.config` را به‌روز کنید.
2. **نسخه‌های پکیج**: Next.js 14 و Prisma 6 استفاده شده؛ برای production، استفاده از آخرین patchها توصیه می‌شود.
3. **CI/CD**: workflow Lint برای main و PRها فعال است؛ می‌توان build و test را هم اضافه کرد.
4. **دیتابیس**: اتصال به Neon PostgreSQL وجود دارد؛ اگر Prisma Migrate استفاده نشود، schema با دیتابیس ممکن است از هم فاصله بگیرد.

---

## ۸. نتیجه‌گیری

پروژه **Caremon** یک MVP برای اجاره خودرو است با UI نسبتاً کامل اما بدون backend واقعی برای احراز هویت، رزرو و مدیریت خودروها. اصلی‌ترین اولویت‌ها عبارت‌اند از:

1. رفع مسائل امنیتی
2. تکمیل احراز هویت
3. پیاده‌سازی جریان رزرو و پرداخت
4. انتقال دیتای خودروها به دیتابیس

با اجرای فازهای پیشنهادی می‌توان به یک سرویس قابل استفاده و مقیاس‌پذیر رسید.
